# ============================================================
# Requirements — Object Detection Under Visual Degradation
# Python 3.11, CUDA 12.x (local dev + Google Colab Pro A100)
# ============================================================

# --- Core detection frameworks ---
ultralytics==8.4.41         # YOLOv11, YOLOv12, RT-DETR
rfdetr==1.6.5               # RF-DETR (requires Python >=3.10)

# --- Deep learning (pre-installed on Colab; do not hard-pin) ---
torch>=2.5.0
torchvision>=0.19.0

# --- Computer vision ---
opencv-python==4.13.0.92

# --- Plotting and analysis ---
matplotlib==3.10.9
seaborn==0.13.2
pandas>=2.2.3               # 3.x works on Python 3.11; 2.2.3 is the minimum
numpy>=2.0.2                # 2.x required; 2.3.x is fine on Python 3.11

# --- FLOPs and parameter counting ---
# NOTE: fvcore is unmaintained (last update Dec 2022) but still functional.
fvcore==0.1.5.post20221221

# --- Hardware monitoring ---
psutil==7.2.2
nvidia-ml-py==13.595.45     # Replaces deprecated pynvml; same import pynvml API

# --- Utilities ---
tqdm==4.67.3
PyYAML==6.0.3
python-dotenv==1.2.2
Pillow==12.2.0